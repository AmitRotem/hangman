# hangman todo list

* JS and CSS output a version number and display on both the welcome and game page, so we can ensure we don't load new HTML with old assets.

## still to verify
* on the (old) Android tablet, make sure the hidden word does not flash a typed letter before it becomes a dot. 

## latest test status
* local `smoke-tests.html`: 5 / 5 passed.
* published `smoke-tests.html`: 5 / 5 passed.
* local app test: full screen button is present, grouped words render as 4 word groups for `HELLO, WORLD 123 TEST`, and the narrow mobile layout fit check passed.